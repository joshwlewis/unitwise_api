{
  "name": "Unitwise API",
  "description": "A Rest JSON API for unit conversions",
  "addons": ["heroku-redis"],
  "buildpacks": [{ "url": "heroku/ruby" }],
  "environments": {
    "test": {
      "addons": [],
      "scripts": {
        "test": "bundle exec rake"
      }
    }
  }
}
